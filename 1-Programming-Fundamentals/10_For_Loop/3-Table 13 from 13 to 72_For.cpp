#include <iostream>

using namespace std;

int main() {
    int multiplicand = 13, product = 0;

    // Using a for loop with i to control the loop
    for (int i = 13; i <= 72; i++) {
        product = 13 * i;
        cout << "13 * " << i << " = " << product << endl;
    }

    return 0;
}
