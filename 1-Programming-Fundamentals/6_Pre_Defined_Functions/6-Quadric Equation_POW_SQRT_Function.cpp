#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int a = 5, b = -9, c = -2;
    double d = sqrt(pow(b, 2) + 40);
    cout << "The First value of x: " << (9 - d) / 10 << endl;
    cout << "The Second value of x: " << (9 + d) / 10 << endl;
    return 0;
}
